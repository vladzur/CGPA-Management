{
  "name": "cgpa-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm --parallel -r lint"
  },
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=9.0.0"
  },
  "pnpm": {
    "overrides": {
      "serialize-javascript": ">=7.0.5",
      "uuid": ">=14.0.0",
      "@tootallnate/once": ">=3.0.1"
    }
  }
}
